import { repeatText } from '../../../helpers/repeat-text';
import { module, test } from 'qunit';

module('Unit | Helper | repeat text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = repeatText(42);
  assert.ok(result);
});
