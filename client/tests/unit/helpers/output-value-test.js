import { outputValue } from '../../../helpers/output-value';
import { module, test } from 'qunit';

module('Unit | Helper | output value');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = outputValue(42);
  assert.ok(result);
});
