{{title "Administration Panel"}}
<h1>Administration</h1>

<div class="administration">

    <div class="menu">
        <ul>
            {{#each model as |menuItem|}}
            <li>
                <a>{{menuItem.title}}</a>
            </li>
            {{/each}}
        </ul>
    </div>
    <div class="page">
        {{outlet}}
    </div>

</div>
